# Termux Neo 0.9.0-beta Release Notes

Release version: `0.9.0-beta`

Prospective tag: `v0.9.0-beta`

Archive: `termux-neo-0.9.0-beta.tar.gz`

Publication status: checkpoint only; no Git tag or public release.

## Verified changes

- Completed the feature-frozen public-beta lifecycle and fallback matrix,
  including clean install, preserved-settings update, and both uninstall
  modes.
- Verified the external checksum, complete internal manifest, extracted
  artifact smoke, repeated-render stability, and Git-independent lifecycle.
- Recorded zero open critical security defects and zero open
  release-blocking defects.
- Published the MIT-licensed public documentation set and deterministic
  `neo` and `matrix` renderer captures.
- Established strict Semantic Versioning ownership, a reviewed package
  allowlist, version-derived notes and tag metadata, and reproducible
  clean-checkout package verification.

## Evidence scope

The physical record covers one Samsung Galaxy Note5 (`SM-N920C`) running
Android `11`, with observed terminal widths of `56` and `94` columns. Portable
fixtures are not evidence for additional devices, Android releases, or Termux
distribution channels.

## Frozen behavior

This checkpoint does not add or change runtime commands, settings, themes,
rendering, lifecycle paths, background behavior, compatibility claims, or the
`VERSION` value.
