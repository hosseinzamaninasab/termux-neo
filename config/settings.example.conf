# Termux Neo settings schema v1
#
# Copy values as plain KEY=value data. Do not use shell quotes, variables,
# command substitutions, or inline comments.
# display_user accepts 1–28 letters, numbers, underscore, dot, and hyphen.

schema_version=1
display_user=Zoro
theme=neo
color_mode=auto
startup_integration=false
